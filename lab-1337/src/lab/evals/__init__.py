"""Lab evals - Grading functions for experiment tasks.

Each eval module provides a grade_implementation() function that:
1. Takes the implemented function as argument
2. Returns a dict with scores by level (basic, edge_cases, security, etc.)
"""
