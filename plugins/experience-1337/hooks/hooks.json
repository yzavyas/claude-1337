{
  "description": "Content structure validation: checks markdown files for proper linking and structure",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/validate-content.sh"
          }
        ]
      }
    ]
  }
}
